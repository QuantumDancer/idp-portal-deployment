apiVersion: v1
kind: Service
metadata:
  name: {{ include "idp-portal-deployment.fullname" . }}
  labels:
    {{- include "idp-portal-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "idp-portal-deployment.selectorLabels" . | nindent 4 }}
